let arr = [1, 2, 3, "black", "white"];

let arrObj = [10, 20, 30, { name: "admin" }, { name: 40 }];

// array
let restrictArr: number[] = [100, 200, 300, 400, 500];
let restrictStr: string[] = ["100", "200", "300", "400", "500"];

// tuple
let restrictTup: [string, number, number, boolean] = ["black", 100, 200, true];
